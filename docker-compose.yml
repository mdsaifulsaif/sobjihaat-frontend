services:
  frontend:
    build: .
    container_name: sobjihaat-frontend

    ports:
      - "3000:3000"

    volumes:
      - .:/app
      - /app/node_modules

    env_file:
      - .env.local

    command: npm run dev